import React from 'react';
import ReactDOM from 'react-dom';
import {App} from './App';

ReactDOM.render(
  <h1>Looks like there's something wrong with your index.js....</h1>,
  document.getElementById('root')
);
